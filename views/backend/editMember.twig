{% extends "backend/base.html.twig" %}

{% block body %}
  <div class="center">
    <div class="animated flipInY milky backTitleSize">クランメンバーの編集</div>
    <a class="hvr-ripple-in editMemberButton" data-remodal-target="editMemberModal">メンバー追加</a>
  </div>

  {# メンバー編集ポップアップ #}
  {% include ('popup/editMember.twig') %}

  <script>
    $(document).ready( function(){
      var inst = $('[data-remodal-id=editMemberModal]').remodal();
      {% if isErrer is defined and isErrer == true %}
        inst.open();
      {% endif %}
    });
  </script>
{% endblock %}
