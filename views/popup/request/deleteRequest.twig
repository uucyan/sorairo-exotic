<div class="remodal" data-remodal-id="deleteRequestModal{{loop.index0}}">
  <button data-remodal-action="close" class="remodal-close"></button>
  <h1>入隊申請の削除</h1>
  <p>{{request.name}} の入隊申請を削除しますか？</p>
  <form class="w3-container" action="requestDeleteAction" method="post">
    <input name="id" class="w3-input" type="hidden" value="{{request.id}}">
    <br>
    <button class="remodal-confirm">削除</button>
    <button data-remodal-action="cancel" class="remodal-cancel">キャンセル</button>
  </form>
</div>

<script>
  {# Enterキーでキャンセルボタンが実行されるから仕方なく制御 #}
  $(function(){
    $("input").on("keydown", function(e) {
      if ((e.which && e.which === 13) || (e.keyCode && e.keyCode === 13)) {
        return false;
      } else {
        return true;
      }
    });
  });
</script>
